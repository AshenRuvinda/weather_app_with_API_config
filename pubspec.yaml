name: weather_app
description: A Flutter Weather Application
version: 1.0.0+1

environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.31.0        # ✅ Updated to latest version matching Firebase
  cloud_firestore: ^4.17.0      # ✅ Compatible with latest firebase_core
  cupertino_icons: ^1.0.2
  http: ^0.13.5
  geolocator: ^9.0.2
  intl: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # assets:
  #   - images/
